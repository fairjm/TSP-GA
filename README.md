# TSP-GA
a simple exercise to use GA to calculate TSP promble(not good yet)

The result is very bad now.Use 1000 individuals in population with 10000 generation.(You can change these in `cc.tsp.util.Configuration`
)

The data is from tsplib att48.

Just run the `WorkFlow`.
